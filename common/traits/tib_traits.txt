# Muslim version of the Blood of the Prophet-trait. Muslims agnatically descended from Muhammad. Female version is localized 'Sayyida'
sayyid_quraysh = {
	category = fame
	
	islam_religion_opinion = 5
	
	ruler_designer_cost = 25
	
	name = {
		first_valid = {
			triggered_desc = {
				trigger = { NOT = { exists = this } }
				desc = trait_sayyid_quraysh
			}
			triggered_desc = {
				trigger = { is_female = yes }
				desc = trait_sayyida_quraysh
			}
			desc = trait_sayyid_quraysh
		}
	}

	desc = {
		first_valid = {
			triggered_desc = {
				trigger = { NOT = { exists = this } }
				desc = trait_sayyid_quraysh_desc
			}
			desc = trait_sayyid_quraysh_character_desc
		}
	}

	inherit_chance = 100
	parent_inheritance_sex = male
	inherit_from_real_father = no
}

# Fake Sayyids with shady genealogies
fake_sayyid = {
	category = fame
	
	islam_religion_opinion = 10
	
	ruler_designer_cost = 20
	
	name = {
		first_valid = {
			triggered_desc = {
				trigger = { NOT = { exists = this } }
				desc = trait_sayyid
			}
			triggered_desc = {
				trigger = { is_female = yes }
				desc = trait_sayyida
			}
			desc = trait_sayyid
		}
	}

	desc = {
		first_valid = {
			triggered_desc = {
				trigger = { NOT = { exists = this } }
				desc = trait_fake_sayyid_desc
			}
			desc = trait_fake_sayyid_character_desc
		}
	}

	inherit_chance = 100
	parent_inheritance_sex = male
	inherit_from_real_father = no
}
fake_sayyid_exposed = {
	category = fame
	opposites = {
		sayyid
	}
	islam_religion_opinion = -10
	
	ruler_designer_cost = -15
	
	name = {
		first_valid = {
			triggered_desc = {
				trigger = { NOT = { exists = this } }
				desc = trait_fake_sayyid
			}
			triggered_desc = {
				trigger = { is_female = yes }
				desc = trait_fake_sayyida
			}
			desc = trait_fake_sayyid
		}
	}

	desc = {
		first_valid = {
			triggered_desc = {
				trigger = { NOT = { exists = this } }
				desc = trait_fake_sayyid_exposed_desc
			}
			desc = trait_fake_sayyid_exposed_character_desc
		}
	}

	ai_zeal = -10
	ai_honor = -15
	ai_greed = 15

	inherit_chance = 0 # THE LINE ENDS HERE BOIII
	parent_inheritance_sex = male
	inherit_from_real_father = no
}

salaf_al_salih = { # Muslim pious predeccessors
	category = fame
	monthly_piety = 2
	
	islam_religion_opinion = 50
	
	shown_in_ruler_designer = no
	
	name = {
		first_valid = {
			triggered_desc = {
				trigger = { NOT = { exists = this } }
				desc = trait_salaf_al_salih
			}
			triggered_desc = {
				trigger = { is_female = yes }
				desc = trait_salafa_al_saliha
			}
			desc = trait_salaf_al_salih
		}
	}

	desc = {
		first_valid = {
			triggered_desc = {
				trigger = { NOT = { exists = this } }
				desc = trait_salaf_al_salih_desc
			}
			desc = trait_salaf_al_salih_character_desc
		}
	}

	ai_zeal = 100
	ai_honor = 15

	inherit_chance = 0
	parent_inheritance_sex = male
	inherit_from_real_father = no
}